# book-recommendation
Using K-Nearest Neighbor to create a book recommendation algorithm 

Developed a model using NearestNeighbors from sklearn.neighbors that show books that are similar to a given book. 

The Nearest Neighbors algorithm measures the distance to determine the “closeness” of instances.
